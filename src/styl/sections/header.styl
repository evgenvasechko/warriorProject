.header
    position: fixed;
    top: 0;
    display: block;
    width: 100%;
    background: linear-gradient(-180deg, #FFFFFF 0%, rgba(255,255,255,0.60) 58%, rgba(255,255,255,0.00) 100%);
    height: 10vh;
    z-index: 9;
    &-menu
        margin: .5rem auto
        display: flex;
        flex-direction: row;
        justify-content: space-around;
        align-items: center;
        list-style: none;
        padding: 0;
        max-width: 960px;
        font-size: 1.6rem
        color: #444444
        &__unit
            padding: 1.5rem 2rem;